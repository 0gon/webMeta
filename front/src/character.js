const Position = {
    x: number,
    y: number,
};

const Direction = {
    DOWN: 0,
    UP: 1,
    LEFT: 2,
    RIGHT: 3,
}

class Character {
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}